# 高德更新目标台账

## 2026-09-17 / PLC 接口按原时序与 9 月 11 日协议对齐

- 唯一主要问题：`/home/ubuntu/disk/pj/HousingInspection` 的 008 下位机接口实现明确绑定 9 月 8 日 SRS V6 点表，不能直接作为用户指定的《PLC与上位机通信接口协议》与 11 份《上下位机对接》时序图的正式接口。
- 原始证据与对照：`pj` 未提交的 `IPlcDevice`、`SrsV6PlcDevice`、Host 工程入口和验证脚本；《PLC与上位机通信接口协议.docx》（SHA-256 `a896faf063b5042bd43e0406ed011cb4f233fca37e64ebe1d5cdea11016b6d16`）；仓库的 `docs/process-diagrams/upper-lower-v13/` 11 图与 V1.3 架构。对照版本为干净的 main=`2c9e259`，本轮独立分支 `plc-protocol-20260911-alignment`，不改同学的 `pj` 工作树及 S01 PR #7。
- 预期改变：保留可复现的旧 V6 兼容通道，提供按 9 月 11 日协议的独立地址、Float32、面号和状态/报警合同及安全准入；按时序图修订工程流程与差异清单。未定义的实体动作、取放参数和字序/地址口径必须显式阻断，不能写入猜测值。
- 失败条件：混用 V6 单字坐标与新双字点表；将 V6 模拟器结果称作新协议联调；在未确认字序/坐标/握手时真机发运动；把 Accepted 当物理完成；未说明时序图与协议初始化次序冲突；把测试端口替身或只读探测称为完整整盘通过。
- 前置/案例/预算：已读 `pj` 源码、其 S01 合同、原 DOCX 点表与关键条款、仓库 11 图目录及 V1.3 边界；以首工位 3D→F、翻面重扫、分拣整体取放和故障/断联为核对案例。无付费模型或硬件动作，预算 0 元。执行事实、修改、测试与未决在本节结项补齐。

### 本次结项

- 已确认/已排除：`pj/HousingInspection` 的 008 实码按 SRS V6 十进制一基与 U16 实现，`pj/Virtual PLC` 和仓库模拟器同属旧 V6；9 月 11 日原件要求两字 Float32、Camera/Scan/Grab 三 Z、实际 XYZ、目标/实际面号及报警。`4x000B/000D/000F` 排除“全部数字按十进制”解释；虚拟测试选择十六进制一基，真实设备仍要实测。图要求 3D→F，而协议 §3.1.1 先盘码/区域 ACK 再上料位，按用户指示以图为流程准则。
- 实际修改：从 `pj` 的统一端口/Host 入口和串行 Modbus 客户端提取可用结构，重写为新协议语义端口、点表/四种 Float32 编解码、只读快照、显式回环虚拟动作、面号翻面、真实动作拒绝和分拣整体取放阻断；补时间戳/原始状态码/错误原因及 Host 跨进程只读脚本。旧 V6 探针未改协议。保存新 DOCX 原件和 Spec Kit 009 规格/计划/任务/合同/验证，修正团队派任、旧差异页和测试说明。
- 逐层结果：Linux SDK 10.0.401 锁定还原 PASS、Release 构建 0 警告/0 错误、四程序集 57/57（其中新协议 11/11）；旧 V6 13/13 和独立模拟器 5 场景 PASS，证据 `simulator/artifacts/20260917T131635Z-b05dfc1d/summary.json`；新协议 Host→独立 TCP 测试服务只读 4 次、0 写及动作开关拒绝 PASS，证据 `simulator/artifacts/plc-20260911-host-readonly-final.json`。正式新版虚拟 PLC、Windows 真机、完整整盘 NOT RUN，不能由上述通过推断。首次误用构建 CLI/路径及锁文件待更新导致的失败已在 009 验证记录说明，最终复核通过。
- 未解决与下一增量：设备方签认真实地址/Unit ID/FC、Float32 排列/坐标系/限位、首 3D/F 命令及区域配置次序、按钮/分轴到位、命令序号/停止/重连、翻面整体取放/旋转/分拣双端点与复位责任；下位机同学据新合同交新版独立模拟器和轨迹，中台再按 S01 3D→F 及故障矩阵做真实双进程动作联调。真实设备动作继续禁用，旧协议测试不折算新协议。付费调用 0 次，费用 0 元；本轮仅软件与文档，无真机命令或外部消息。
- 位置：独立分支 `plc-protocol-20260911-alignment`，首提交 `6bfa1de`，[Draft PR #9](https://github.com/NickYoung618/x/pull/9)。PR CI 结果待同提交复核；未经设备合同签认不将 Draft 转正式合并。

## 2026-09-17 / 全工位下位机并行先行

- 唯一主要问题：逐工位等待下位机交接口会串行拖慢开发；按用户最新要求，将设备侧全流程合同、虚拟 PLC 和独立接口模拟前置，让中台保持逐工位 PR 开发。
- 原始证据与对照：`/home/ubuntu/disk/yyh/架构设计.zip` 内 `上下位机对接.zip` 的 11 组图（外层 SHA-256 `cc5250a9b1e322979e1def987f19ff1c01cd5f4894e93f6152118c726aba0452`，内层 SHA-256 `cdbf8add6e477d8075c8fc756c28683545dc80eb443c4db539edb4b7ed10973a`）、V1.3 §2/5/7/11/13/18、旧 V6 差异表、S01 Draft PR #7；从 `main=99730a4` 建独立协作分支。原 S01 派任按 D1–D3 串行逐站交接。
- 预期改变：把 11 图可复核地收进仓库，编制覆盖安全启动、3D/F/A–D/E、翻面重扫、特殊旋转/搬运、分拣和放行的全工位设备侧清单、版本化合同和并行交付门槛；中台逐工位 PR 不等待设备侧前一工位实现完毕。
- 失败条件：把流程图当点表/真实硬件参数；把 PLC 的职责扩展到配方、算法或相机触发；把 V1.3 明确的翻面后重扫静默删去；用旧 V6 测试冒充全工位 V1.3 或真机通过；在未签认点表时向真机发动作。
- 前置/案例/预算：已读外层和内层 ZIP 清单、11 组 SVG 内容及总时序 137 步、V1.3 相关章节、S01 派任与现有协作方案；本轮固定正常 S01、翻面重扫、特殊件取放和分拣错误作为合同审查样例。无付费模型/设备动作，预算 0 元。工作中的修改、检查与未决在本节结项补齐。

### 本次结项

- 已确认：11 图明确 PLC 管实体安全、夹紧、轴与机构实际执行，上位机管相机触发、3D 定位、配方/算法/判定。总时序列 137 个消息步骤；图未画翻面后 3D 重扫，但 V1.3 明确要求；图仅展开特殊类型 1。现有 S01 草案把 3D 模拟器写成直接返回槽位，与图中的“相机给点云/深度、上位机定位”有层次差异，已列为 S01 PR 后续修订。
- 实际修改：将 11 份原始 SVG 与逐文件 SHA-256 及来源索引加入 `docs/process-diagrams/upper-lower-v13/`；新增全工位并行派任书，列 L0–L5、逻辑动作清单、全量合同、模拟/适配、跨进程和真机分层验收；更新团队协作与工位交付顺序，明确下位机前置全量开发、中台仍逐工位 PR。
- 检查：原始两层 ZIP 的 SHA-256 已记录，11 SVG 逐字节 SHA 与 manifest 一致且均可解析；4 份新增/修改 Markdown 的相对链接 0 缺失，`git diff --check` PASS。本轮仅文档与来源副本，未运行新 .NET/设备测试、未发硬件命令；先前旧 V6/S01 调用侧测试不折算本单通过。付费调用 0 次，费用 0 元。
- 未决与下一增量：点表、命令关联/停止/恢复、实体位置/机构参数、3D 采集传输及类型 2–4 配方仍待设备方草案和双方签认。下一步先评审全量合同，同时修订 S01 草案的 3D 分层；下位机可并行开发 L0–L5，S01 接收到已发布版本后做真实 Modbus 与独立相机模拟联调。

## 2026-09-17 / S01 3D 自行模拟与 SDK 接入边界

- 唯一主要问题：当前没有专职 3D 同学，需由中台自行模拟并确保真实相机到来时可替换；先核对现有资料是否有可用参数，以及只有 SDK 的验证上限。
- 原始证据：V1.3 架构 §4.1–4.4、§5.2、§8、§20，S01 规格/计划/合同/矩阵，《技术文件（带目录）0626.pdf》与《PLC与上位机通信接口协议.docx》；当前 `station-01-implementation` Draft PR #6，原工位代码仍未实现。
- 对照与预期：在干净的 `ffedc4c` 基线上，只澄清 S01 责任、模拟/真实 Provider 合同及 SDK 单独到手的验收边界，不猜相机品牌/参数或实施真实采集。
- 失败条件：将 2D 分辨率、历史整机精度或“结构光/激光”字样当 3D 相机参数；只有 SDK 就宣称真机定位/标定/精度通过；真实模式缺依赖时回退合成成功；让中台业务状态机依赖厂商 SDK。
- 前置与预算：已读上述架构、S01 文件及两份原始资料的相机/3D 条目；无设备动作、付费模型或外部费用，预算 0 元。确认的事实、修改、检查及未决项见本节结项。

### 本次结项（合同与资料核对）

- 已确认：V1.3 和原始资料给出 3D 职责与接口需求，没有可签认的型号、视野、工作距离、分辨率、精度、采集参数或标定；`5120×5120` 是 2D 相机指标，历史 `0.01 mm` 是检测/整机要求。原 PDF 称结构光，PLC 文档称激光相机，具体技术未冻结。当前仓库与交接目录未见 3D SDK/真实点云。
- 实际修改：新增 `specs/005-initial-3d-station/3d-adapter-readiness.md`；更新 S01 规格、计划、任务和工位合同，明确中台负责独立 3D 合成端口，真实 SDK Provider 显式替换且不得自动回退。
- 验证：文档变更，`git diff --check` PASS；新增/修改的 S01 文档相对链接检查无缺失。不涉及 .NET 程序，S01 合成、SDK 离线、真实设备与整盘测试均 NOT RUN。无付费调用，费用 0 元。
- 未解决/下一增量：取得准确相机/SDK/固件/运行环境、触发/光源、点云格式、坐标标定、样件预期与空盘规则后做真实适配及合同测试；现在先按 T004–T015 完成合成工位链和故障矩阵。SDK 单独到手仅能做可用 API/离线能力验证，不能替代真机 W0–W5。

## 2026-09-17 / S01 下位机工作派任书

- 唯一主要问题：下位机负责人现在需要一份可直接执行、可回执的工作指令，而现有差异表和 S01 规格分散描述责任、接口及测试。
- 原始证据：`docs/contracts/virtual-plc-alignment.md`、`specs/005-initial-3d-station/{contracts/station-boundary.md,test-matrix.md,plan.md,windows-real-device.md}`；S01 主 Draft PR #6 当前只有方案，无工位代码。
- 对照与预期：从 `station-01-implementation` 提交 `6b0d1f7` 的干净工作树出发，新增 D1 点表、D2 独立模拟器、D3 真实 Modbus 双进程、D4 Windows 真机四个里程碑的正式派任书，明确交付位置、完成判据和回执。不替同学冻结未确认点表或发送任务消息。
- 失败条件：旧 V6 兼容冒充 V1.3；把模拟器 HTTP 控制入口当中台业务通信；无独立设备轨迹/动作次数仍称联调通过；未准入即驱动真实设备；把 PLC 职责扩大到 3D 算法、F/配方或整盘。
- 案例/模型/预算与前置：阅读上述差异/合同/故障矩阵及现场规程，覆盖 P01/P02、M01–M04b 和现场 W0–W5；无付费模型或设备操作，费用 0 元。本次仅生成仓库文档，不代用户向同学发送。

### 派任书结项（文档范围）

- 已形成 [D1–D4 派任书](assignments/s01-plc-work-order-20260917.md)：下位机负责人负责 S01 点表、独立模拟器、Infrastructure V1.3 Modbus 适配与双进程证据，并在现场负责 PLC 版本/状态/轨迹；中台负责 Application/Host 和跨端口验收，3D 结果由设备负责人提供。
- 检查：Spec Kit 前置 PASS、派任书及引用文档的本地链接 0 缺失、`git diff --check` PASS。未修改运行代码，本地 .NET、V1.3 双进程、Windows 现场与整盘均 NOT RUN；旧协议既有通过不计为本单验收。无付费调用，费用 0 元。
- 未解决：正式点表与握手签认、公共 3D 位/到位、Windows 真实 PLC 与 3D 标定/测试件。下一步由下位机负责人按 D1→D3 提交子 PR/证据，中台将其纳入 S01 主 Draft PR；D4 按现场准入另行执行。

## 2026-09-17 / S01 服务器—GitHub—Windows 真机开发与测试方案

- 唯一主要问题：首工位已有业务规格与故障矩阵，但缺从 Linux 开发、GitHub CI/候选包到 Windows 实际设备的分层执行方案；PR #4 已合并的只是开工准备，不能继续在同一 PR 补实现。
- 原始证据：`specs/005-initial-3d-station/`、V1.3 §5.2、§11、§13–14、§18、`docs/windows-acceptance.md`、`docs/team-development-and-release.md`、`.github/workflows/{ci,windows-staging}.yml`、main=`1519cfc`；GitHub runner 列表当前为空，现有 staging 工作流校验旧阶段名 `EngineeringFoundation`。
- 对照与范围：从干净的 main=`1519cfc` 建 `station-01-implementation` 分支。本轮只修订 Spec Kit 方案、合同、矩阵、运行指南及任务，不实现工位代码或发设备命令；旧协议回归、正式 V1.3、Windows 真机与整盘测试分别判定。
- 预期改变：形成可执行的 Linux 本地门禁、GitHub PR/main 同提交候选包门禁、Windows 实机分步测试与证据/失败处理；明确软件合并与真机验收两个不同结论。
- 失败条件：把准备 PR 或绿色旧协议 CI 当工位通过；Windows 托管 runner 当现场设备；未冻结点表/标定即发送真机动作；重新编译不同包冒充同一候选；无设备独立轨迹或失败报告仍写 PASS。
- 前置与预算：已读 S01 规格/计划/矩阵、V1.3 指定章节、Windows 现有方案/工作流及章程；只做仓库文档/接口设计，无真实零件、外部模型调用或费用，预算 0 元。真实 Windows 机型、PLC/3D SDK、现场操作窗口和接口确认均作为现场准入，不以缺失阻断本轮方案。

### 本次方案结项（仅文档）

- 已确认：PR #4 已合并准备文档而未实现 S01；当前无仓库自有 Windows runner，现有 staging 工作流的旧阶段名与框架 CI manifest 不符。Windows 托管 runner 与用户实际 Windows 电脑/设备现场必须分开。
- 实际修改：扩展 `specs/005-initial-3d-station/` 的规格、方案、研究、数据/对外合同、快速启动、任务和测试矩阵；新增现场规程与本轮方案核验；更新 V1.3 验收映射的框架已通过事实。软件可合并 G0–G3、main 同包 G4/G5、真机 G6 分开。
- 已运行：Spec Kit 前置检查 PASS、文档相对链接检查 PASS、`git diff --check` PASS。此为文档变更，本地未重跑 .NET/前端/旧协议；PR CI 结果另以对应 SHA 的检查为准。首工位、V1.3、Windows 真机或整盘未执行，不把历史 CI 误作本轮实测。无付费调用，费用 0 元。
- 未解决：点表/命令关联、公共 3D 位/到位、3D SDK/坐标/标定/样件、Windows 实际系统与现场操作窗口；缺这些不阻止服务器/CI 合成实现，但阻止 G6 真机验收。下一目标是在本分支建立 S01 实现 Draft PR，按 T004–T020 分层开发、修正和测试，真机输入晚到则另开 S01 设备对接 PR。

## 2026-09-17 / S01 开工准备：初始 3D 定位工位

- 唯一主要问题：现有 `TrayWorkflow` 有内存 3D 请求/反馈，却在 `DeviceReady` 后直接请求扫描；Host 没有执行入口，也无“移至 3D 位 → 完成 → 扫描”的工位合同。按工位交付前须先冻结首阶段边界与可检查门槛。
- 原始证据：V1.3 第 5.1–5.3、7、13–14、18 节，`backend/src/Inspection.Application/Workflow/{TrayWorkflow,WorkflowContracts}.cs`、Host 默认状态、003/004/005 验证记录。
- 对照：`main` 合并 PR #1/#2/#3 后提交 `09904fd`；新分支 `station-01-initial-3d`，开工前工作区干净。原 006 草稿分支、stash 和生成产物已按用户要求清理，未进入 `main`。
- 预期改变：先形成首工位 Spec Kit 规格、合同、测试/故障矩阵及团队交接要求；下次在同一工位分支实现并形成单独 PR。主流程仅到“初始 3D 有效、等待 F”，不假装完成 F、配方、算法或整盘。
- 失败条件：未到 3D 位就采集；旧或未知反馈推进；无可靠身份/坐标仍进入 F；合成坐标当真实标定；旧 V6.0 单动作通过冒充正式 V1.3 或真机通过。
- 案例/模型/预算：有/无/重复槽、移动拒绝/未知、扫描超时/迟到；仅确定性测试数据，无付费模型，预算 0 元。前置已读交接、章程、源码、现有测试与 V1.3 对应章节；正式点表和真实 SDK 均未冻结，本阶段以版本化端口保持待接。

### S01 开工准备结项

- 已确认：合并后 `main` 为 `09904fd`，Linux/Windows Core 与 Virtual PLC framework 四个 CI 作业均 SUCCESS；它们仍是现有框架/旧 V6 验证。首工位源码尚未实现，现有 `OnReady` 会直接请求扫描，Host 无执行入口。
- 实际修改：新增 `specs/005-initial-3d-station/` 的规格、计划、研究、数据模型、端口合同、精确故障矩阵、任务、开工核验；新增 `docs/station-delivery-order.md`，更新协作文档当前基线。仅文档，不包含新的工位代码或运行时测试。
- 各轮/结果：Spec Kit 前置脚本通过，`git diff --check` 通过；主分支 CI [35187291769](https://github.com/NickYoung618/x/actions/runs/35187291769) 四作业通过。本轮无新增 .NET 本地运行；S01 合成联调、V1.3 PLC、真实 3D、完整整盘均 NOT RUN，不能引用旧成绩替代。
- 未解决：正式点表与动作/反馈语义、公共 3D 位和到位判据、真实 SDK 坐标系/单位/标定、空盘规则、关键保存与重启对账的生产实现。上述决议不阻止合成端口和工位状态链开发，但阻止真机/V1.3 通过宣称。
- 费用与下一目标：无付费调用，0 元；在同一分支按 T004–T015 完成首工位实现和独立证据，PR 保持 Draft 直至实现与矩阵通过，然后请前端/下位机同学按合同复测。

### S01 基线更新（框架 PR #5 合并后）

- PR #5 已合并为 main=`d03068490de58ad7511cf692e805867bf8330d7b`，合并后 [main CI 35192471611](https://github.com/NickYoung618/x/actions/runs/35192471611) 六作业成功；S01 草稿分支合并该基线，并保留首工位原始准备证据。合并文档冲突时同时保留框架和 S01 台账，协作文档按当前能力更新。
- 本次只对齐基线/文档，不实现工位，S01 合成联调、V1.3 正式 PLC 与完整整盘仍 `NOT RUN`。下一轮在同一 Draft PR 实现 T004–T015 并逐项跑故障矩阵。
## 2026-09-17 / V1.3 整体框架重构开工

- 唯一主要问题：现有后端已出现计划、内存工作流及旧协议工程探针，但尚缺对照 V1.3 的完整模块边界、运行模式、统一对外合同与分层验收门槛；直接实现 S01 会把框架和工位功能混在同一 PR。
- 原始证据：`docs/architecture/architecture-v1.3.md` 第 3–4、6–14、17–18 节，现有四个后端项目、Host `/api/system/status`、003 内存工作流、002/004 旧协议验证，S01 Draft PR #4。
- 对照：合并后 `main`=`09904fd`，工作区干净；新分支 `framework-v13-foundation` 从 main 建立。既有 PR #1/#2/#3 已合并，PR #4 保持 Draft 且不在框架前合并。
- 预期改变：按 Spec Kit 另立框架规格/计划/任务；建立编译可用且默认拒绝未实现业务的 V1.3 外部合同、15 责任模块目录、唯一 Host 组合与能力状态，明确后续逐工位 PR 的测试门槛。不实现 3D/F/A/B/C/D/E 具体动作、配方、算法或判定。
- 失败条件：空接口或模拟成功被当成生产能力；框架 PR 启用物理动作；旧 V6 结果冒充 V1.3 或完整整盘；把 15 责任模块实现成 15 个强制进程/项目；未实现能力返回“生产就绪”。
- 案例/模型/预算：检查模块/运行模式/能力状态、未实现命令拒绝、依赖方向及已有回归；无真实零件或付费模型，预算 0 元。前置已读 V1.3、章程、当前项目/Host/测试、交接及 S01 规格；本阶段不冻结硬件点表与算法细节。

### V1.3 框架重构结项（本地验证范围）

- 已确认：V1.3 十五责任模块映射与现有四工程/唯一 Host 可以并存；旧内存流程和旧 V6 工程探针不构成工位实现。P0 最小 Vue 状态页已建并能经 Vite 代理读取发布 Host；完整 P0 退出还需真实浏览器/Windows 交互及三方接口核对。
- 实际修改：新增纯外部 DTO 项目 `Inspection.Contracts`、Application 模块/能力目录、Host 状态与 501 默认拒绝、项目依赖/HTTP 检查、发布进程冒烟断言、Vue/TypeScript/Pinia 只读最小状态页及 Linux/Windows CI、Spec Kit 007 和 V1.3 §18 全场景/DB 专项映射；未新增设备动作、配方、算法或数据库迁移。
- 各轮：新增 Host 合同测试首跑 2 项失败（旧阶段、404），实现后旧阶段断言 1 项失败，修正后 7/7。随后发现旧能力 ID 兼容风险并保留旧 ID；补无设备 Provider 绑定检查，最终 Host 8/8、全量 46/46。Linux 锁定还原、Release 构建 0 警告/错误、发布进程 HTTP 冒烟通过；旧协议 13/13 和双进程 5/5 通过。证据 `specs/007-v13-framework-foundation/validation.md` 与 `evidence/legacy-framework-summary.json`。
- 新失败/修正：前端首次选用 TypeScript 7.0.2 时 vue-tsc 无法加载旧内部路径；锁定 TypeScript 5.9.3 后 `npm ci`、类型检查、3/3 组件测试和离线构建通过。补强 API 格式校验后类型检查发现过宽断言，逐字段构造后复测通过。
- PR CI：首次运行 35191181805 的旧协议两作业 PASS；Core 两平台在 PowerShell 发布冒烟的 501 合并断言失败，旧断言未输出子条件，不能直接认定 Host 返回错误；UI 两平台的锁文件引用本机腾讯镜像域名，托管 runner 解析失败。已把 156 个锁定下载 URL 改为官方 registry 并用空缓存 `npm ci` 验证；PowerShell 改为分别获取响应体和状态码。修正后运行 35192043297 的 Linux/Windows Core、Framework UI、Virtual PLC framework 六作业全部 PASS。首次 PowerShell 失败的精确子条件因证据不足仍未定，不捏造根因。
- 未解决：V1.3 PLC 点表与动作关联、真实 3D/相机 SDK、前端真实浏览器/WPF 集成与三方接口核对、正式配方/算法、数据库部署与恢复；V1.3 正式对接和完整整盘均 NOT RUN。旧协议脚本实测发生在最后只读状态/前端补充前，CI 将对最终提交重跑。
- 远端与费用：框架已推送 `framework-v13-foundation` 并建立 [PR #5](https://github.com/NickYoung618/x/pull/5)；最终提交 `576778b` 的 Linux/Windows 六作业 CI 全部通过。无付费调用，0 元。
- 下一目标：框架 PR 合并后，将 S01 Draft PR #4 更新到该基线，只实现首个 3D 工位并按其正常/故障矩阵测试；前端同学继续完整页面与 WPF，不把最小页面当成桌面验收。

## 2026-09-17 / 005 开工：全虚拟覆盖策略校正

- 唯一主要问题：现有测试路线以 2 件×2 面普通件样例呈现，未明确 P1 完整验收须覆盖全部已定义类型、发布配方和故障族，容易将框架样例通过误读成全量通过。
- 原始证据：`docs/testing.md` 第 3 节、`docs/ordinary-tray-roadmap.md`、V1.3 第 5.4、13、14、18 节，以及《检测场景及零件整理(20260313).xlsx》“场景及物料”表。
- 对照：`4987d51`，004 分支工作区干净；新分支 `005-fullsim-coverage-strategy` 基于 004。
- 预期改变：建立“型号/场景/物料 → 已发布配方 → 工艺模板/步骤 → 故障族 → 独立断言/证据”的覆盖清单和完成门槛，明确开发样例、P1 全虚拟、P2 真实算法和现场验收的边界。
- 失败条件：把样例或旧协议测试计作全量；虚构真实生产配方或未定特殊步骤；遗漏已发布配方；以未运行、跳过或只有上位机日志的案例宣称通过。
- 案例/模型/预算：本轮仅阅读本地规格与场景矩阵、修订测试策略，不运行付费模型或真实设备，预算 0 元。
- 前置阅读：已读 V1.3 工艺/仿真/验收、当前测试计划及路线、决策表，核对 Excel 表头与型号行。表格 21–27 列存在未标表头的“有”，不能自行归入正式配方。

## 005 结项（覆盖计划范围）

- 已确认：源表 SHA-256 `2d3d0494b960fe04dab5fc997bdb7a2d9b9c6406d3cfe23b060d32e5592657c3`；有表头 B:T 区域包含 9 型号、3 场景、100 个“有”的候选组合，X:AA 的额外“有”无可读表头。源表不是完整配方目录，也未给出特殊模板映射。
- 实际修改：新增 `docs/fullsim-coverage-plan.md` 与 100 行来源清单 `docs/coverage/model-scenario-material.csv`；修订 `docs/testing.md`、`docs/ordinary-tray-roadmap.md`、`docs/decisions.md`，将 2×2×AB 明确为开发切片、P1 明确为全类型/已发布配方/适用故障验收。
- 验证：CSV 100 行与 100 个不同源单元格，型号分布 12×5+10×4，场景分布 50/28/22；`git diff --check` 通过。本轮只改文档/清单，本地未重跑 .NET；CI 重跑既有框架检查，V1.3/完整整盘未运行。
- 远端：`005-fullsim-coverage-strategy` 已推送，草稿 PR #3 指向 004；首提交 `d060273` 的 [CI 运行](https://github.com/NickYoung618/x/actions/runs/35183581811) 四个 Ubuntu/Windows Core 与 Virtual PLC framework 作业均 PASS，仍仅验证既有旧协议和框架，非 V1.3/全配方测试。
- 未解决：正式配方集、源表 X:AA 含义、型号/物料到模板映射、类型 4 步骤、下位机 V1.3 接口与真实算法/现场验收。缺失项不得计入通过。
- 费用与下一目标：无外部或付费模型调用，0 元。下一增量按覆盖计划完善正式配方目录/用例索引和业务执行端口；下位机接口确认后独立补正式协议测试。

## 2026-09-17 / 004 开工：给下位机同学的候选联调框架

- 唯一主要问题：002 的统一入口锁死“三份TRX”，003 新增第四个测试工程后误失败；下位机同学也无法用同一入口明确测试独立发布的候选模拟器。
- 原始证据：`scripts/validate-virtual-plc.py` 的固定数量断言、`Gaode.slnx` 四个测试工程、002/003验证及协议差异表。
- 对照：提交542c894，工作区干净；分支004-plc-candidate-testkit，基于003。
- 预期改变：按解决方案和TRX对账；显式旧协议候选DLL模式、来源哈希、独立设备证据和可运行交接指南。
- 失败条件：少测/跳过仍为PASS，测试候选却实际启动内置模拟器，向远端/真机发送旧协议动作，把旧协议通过写成V1.3或整盘通过。
- 案例与模型：当前旧协议模拟器作为已知候选，另做错误输入负向；无付费模型，预算0元。
- 前置：已读交接、V1.3章程、002/003规格与验证、脚本和Host探针；接口决定仍归下位机同学，本轮不冻结。

## 004 结项（候选测试框架范围）

- 已确认：旧固定三份TRX门禁确实与003四工程冲突；改为按解决方案项目及每份TRX程序集身份对账，四个门禁单元检查通过。
- 运行结果：Linux默认入口构建/发布、42/42、旧13/13、双进程5/5通过；外部候选最终入口同样通过并记录DLL及17文件目录哈希。3个错误输入均返回1，未启动动作。证据见`specs/004-plc-candidate-testkit/validation.md`和`evidence/`。
- 各轮：候选首次通过后增加发布目录整体哈希；复核发现候选驱动将仓库构建误标为候选构建PASS，修正为NOT RUN且加输出断言；最终候选及默认模式分别完整重跑通过。旧报告、最终报告和拒绝目录均保留；旧测试中的明确失败和超时是预期注入。
- 修改：`scripts/validate-virtual-plc.py`、Python门禁测试、CI作业、004 Spec Kit文档及使用指南；未修改同学模拟器源码或未定点表。
- 远端：已推送002和004分支并建立草稿PR #1、#2；运行35182340667及35182357122各四作业SUCCESS，002两平台30/30、004两平台42/42，旧13/13与双进程5/5均有上传证据。004 PR基于002，先审002再审004；尚未合并或发布。
- 未解决：正式V1.3点表/动作关联与整盘、真实Windows交互验收未运行；候选源码构建须由提供者附独立记录。旧合同测试不充当V1.3验收。
- 费用：无付费请求，0元。下一目标：下位机同学以此包回归旧基线并提交正式V1.3合同后，补独立V1.3联调和中台运动适配。

## 2026-09-17 / 002 开工

- 唯一主要问题：现有虚拟PLC未纳入可复现生产构建及中台真实通信验证。
- 原始证据：handover-20260917.md、reviews/virtual-plc-20260917/、架构V1.3、本机快照。
- 对照：f6f7078（工作区干净，含交接；main基线02ca9d3）；独立分支002-virtual-plc-integration。
- 预期：同一SDK生产构建+旧协议检查+两进程兼容联调；明确V1.3差异。
- 失败条件：构建未跑却通过、残留完成推进、未知重发、HTTP替代正常设备通信、把旧检查写为整盘通过。
- 场景：独立模拟器正常/残留/失败/超时；不涉及专利案例或付费模型。拟用付费请求0、费用0。
- 前置：已读交接/提示词/审查/共同章程/已确认决策/V1.3相关全文；快照SHA吻合且隔离解压。
- 范围：本轮仅第一增量。专利项目台账不修改，所有高德变更保存在当前仓库。
- 已确认：旧动作清零不清完成反馈；原测试net8.0，主程序net10.0；本机SDK已安装但未在PATH。
- 未完成：本轮构建测试、协议逐项对照及结果登记。

- 用户范围确认：接口决定交由下位机同学后续处理，本轮先搭框架；保留最小旧协议联调用于框架验证，不扩展新生产协议。

## 002 结项（框架范围）

- 已完成15任务：来源引入、net10/C#12驱动、真实Modbus客户端/Host工程探针、统一脚本及双平台CI配置。
- 实测：Linux生产构建/发布0警告0错误；基础19+通信11共30项通过；旧13组通过；5个独立进程场景通过；默认发布Host仍只预览。
- 各轮：首轮目录式publish驱动失败MSB1050，未跑后续；指定csproj后完整通过；缺失SDK负向测试正确FAIL/NOT RUN。没有丢弃失败记录。
- 原问题：构建依赖.NET8与NOT RUN假通过在新入口已消除；原历史入口保留并显式标旧用法。旧随机质量策略未改，未接业务；不能称V1.3全部修好。
- 未解决：新点表、字序/单位、session/sequence、完整取放与恢复，由下位机同学后续确认。本次不扩大范围。
- 新风险边界：兼容Busy可能被漏采，保守Unknown；不用于生产，不承诺跨重启去重；Windows/新CI尚未实际运行。
- 原文件哈希核对36项，业务源和原13组断言未改；只改测试项目框架/语言，旧SDK文件保留原文。
- 证据与文件清单：specs/002-virtual-plc-integration/validation.md及evidence/；任务均有验证映射。
- 调用/费用：无付费模型请求，0元；本机测试无新增外部付费调用。
- 下一目标：先Application流程/运动资源/采集算法端口与终态框架；协议待负责人确认后接入，不逐项猜生产寄存器。

## 2026-09-17 / 003 开工：普通整盘中台流程框架

- 唯一主要问题：现有中台只有计划预览与PLC工程探针，缺少可把准备、运动、采集、算法技术终态串接起来的Application状态所有者，因而不能逐步验证整盘顺序和失败收敛。
- 原始证据：backend/src/Inspection.Application/Engineering/DemoPlanService.cs、Domain/Planning/OrdinaryTrayPlanner.cs、docs/ordinary-tray-roadmap.md、V1.3第5—14节；对照提交85dafc3，分支003-ordinary-workflow-foundation。
- 预期改变：事件驱动的短状态迁移、一个运动执行通道、明确设备/采集/算法端口、稳定身份和CoordinateEpoch；测试用确定性假设备证明2件2面AB顺序、失败/迟到/未知不推进。
- 失败条件：F扫码前选方案；翻面后沿用旧坐标；旧动作完成推进新动作；普通缺帧全盘停或漏终态；未知动作继续；无真实保存却声称放行或整盘通过。
- 案例/模型/预算：2件2面AB为开发样例，无付费模型/外部请求，预计0元。前置已读002交接与验证、V1.3对应章节、当前规划器与章程；本轮无专利真实案例效果测试。
- 范围：仅中台Application层及可运行的接口级测试，不冻结下位机点表，不挂生产API，不实现持久保存/质量/分拣。用户明确先搭框架，下位机协议由该同学后续确认。
- 开工状态：当前工作区干净；002本地提交85dafc3已通过Linux相关实测，Windows/新CI仍未执行。

## 003 结项（Application框架范围）

- 已确认：普通2件2面AB可经短事件/效果推进到AwaitingDecision；无生产判定/保存/放行。单运动通道有界、动作ID去重、期限到Unknown并阻断；普通缺帧/算法失败与截止形成技术终态。
- 排除：本轮没有修改PLC点表/主机实际运动入口；旧协议13组的002成绩不能算003接口级测试，也不能充作完整整盘通过。
- 各轮失败：首轮并行MSBuild子节点异常，原因未证；单进程暴露两处测试分析器错误并修复；最终默认并行构建复跑通过，首次子节点退出根因未明；首轮完整测试因假设备不服从取消而卡住，通道改用自有期限后通过；非法运动结果码与事件接收端故障也已fail-closed。原尝试日志均保存。
- 实际修改：Application/Workflow与Motion、12个新增Application测试、Gaode.slnx、Spec Kit 003、README/docs/testing及本台账。范围仍为进程内框架；未修改原模拟器。
- 结果与证据：specs/003-ordinary-workflow-foundation/validation.md、evidence/、TRX；Linux构建0警告0错误、42/42测试通过、0跳过；TRX四份和失败尝试均在结项报告。
- 未解决：跨重启恢复、完整PLC命令关联、真实3D/F/帧/算法端口、媒体/数据库与分拣放行、Windows/CI实际运行。翻面后跨槽位身份映射尚无可靠机制，本框架暂要求原槽位集合。
- 费用：无付费模型请求/外部付费服务，0元。
- 下一目标：Host任务入口与真实各端口测试实现、容量/关键保存先行；PLC新协议由负责下位机的同学确认后接入。

## 2026-09-17 / 010 开工：V1.3 全工位合同及独立设备模拟

- 工作目录严格限于 `/home/ubuntu/disk/pj`；现有 008 未提交文件已备份并暂存，当前 `HousingInspection` 分支从 PR #9 的 `3bed65e` 建立。GitHub 不执行合并。
- 本轮合同先行：[L0–L5 全工位草案](contracts/plc-v13-all-stations-v0.1.md)已作为独立提交 `9d382ee`。先按图确定动作语义，再把 9 月 11 日原协议点位与线缆缺口逐项标开；3D/F 命令身份、首次读盘码前区域配置、实际坐标/字序仍待签认。
- 已实施的第一批基础模拟只覆盖显式 9 月 11 日合成协议的点表、Float32 双字、心跳、Ready、夹紧、区域配置、人工区/报警故障和独立原始报文轨迹；V6 默认入口继续隔离。当前新模拟器拒绝未定义的 3D/F 原始运动命令，不把 Modbus 写 ACK 当动作完成。
- 当前验证：.NET 10 锁定还原通过；Release 全解决方案 0 警告/错误；57/57 单元测试；旧 V6 独立脚本 13/13；新协议 Host 与模拟器双进程基础报文/故障验证通过。证据和未运行层级见 [010 验证记录](../specs/010-v13-device-simulator/validation.md)。
- 下一门槛：确认只限回环测试的动作关联承载和独立 3D 传输，再实现同一 Host S01 的 3D/F Accepted/Completed、独立采集、故障/断线/重启对账。Windows 真机与完整整盘保持 `NOT RUN`。
- 远端：[Draft PR #10](https://github.com/NickYoung618/x/pull/10) 以 PR #9 的协议分支为 base，仅供评审，未合并；后续增量仍在此分支实施并复测。
