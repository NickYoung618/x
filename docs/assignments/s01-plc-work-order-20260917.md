# 高德缺陷检测中台 S01 下位机工作派任书

**派任编号**：GAODE-S01-PLC-20260917

**执行方**：虚拟下位机／PLC 软件负责人

**协作方**：中台负责人、设备供方、现场设备操作员

**对应增量**：[S01 初始 3D 定位](../../specs/005-initial-3d-station/spec.md)，[实现草稿 PR #7](https://github.com/NickYoung618/x/pull/7)；PR #6 已合并的是准备文档
**当前状态**：本单为待执行任务；旧 V6 模拟器兼容测试已通过，正式 V1.3 点表、S01 联调和真机验收均未通过。

## 一、目标和边界

交付能供中台通过**实际 Modbus 客户端**接入的 S01 下位机合同、独立虚拟 PLC 及 V1.3 协议适配，并交付独立的 **3D 设备接口模拟**。S01 只处理本次会话的新鲜 Ready／夹紧／互锁、公共准备动作 MoveTo3D 的受理与物理完成、拒绝／超时／断线／停止／重连后的状态与对账，以及到位后按请求返回合成 3D 结果或确定性故障。下位机负责人实现设备侧和 `Inspection.Infrastructure` 的 PLC 协议适配，中台负责人评审应用端口并装配 Host；中台在可信到位后调用 3D，最终停在等待 F。3D 模拟需提供合成槽位/姿态/来源，不承担真实点云定位算法、F 读码、配方、翻面、分拣或整盘业务。

现有模拟器依据旧 V6 点表。不得把旧的单寄存器坐标、残留完成位或随机 `flow-decision` 结果作为 V1.3 生产能力。[差异记录](../contracts/virtual-plc-alignment.md)是逐项核对起点，不是已经双方签认的新点表。

## 二、按依赖顺序交付

| 里程碑 | 下位机负责人具体工作 | 必须交付的材料 | 完成判据 |
| --- | --- | --- | --- |
| D1 协议定版草案；先于中台真实 Modbus 适配 | 对照旧模拟器实码、2026-09-11 接口资料和 V1.3 §7/§11，提出 **S01 专用**版本化点表与时序。逐项确定地址为十进制/十六进制及零基/一基、功能码、寄存器宽度、Float32 字／字节序、符号/单位/范围、公共 3D 目标、Ready/夹紧/安全状态及新鲜度、Accepted/Completed/Failed/错误码、心跳/软停止、命令参数锁存与清零、重启/重连对账。明确采用 Session/CommandSequence，或受限的单动作握手及其限制。 | `docs/contracts/virtual-plc-v13-s01.md` 草案；旧→新字段差异表；至少一组已知数值的寄存器原始字节与期望；时序图；未决项、建议决策和双方签认栏。 | 中台和下位机能用同一组报文独立算出地址、数值和动作终态；未定字段标“待确认”，不填推测值。未签认前不得用新点表驱动真实设备。 |
| D2 独立虚拟 PLC；先于 S01 跨进程验收 | 复用 `simulator/VirtualPlc/` 已有代码，按已确认 S01 合同实现本次 Ready/夹紧/互锁、MoveTo3D 参数锁存、受理、运行、到位、明确拒绝和 Unknown。提供确定性场景/故障注入、独立端口、启动/停止/复位、可配置超时与轨迹导出。保留旧 V6 兼容测试与新合同测试的版本隔离。 | .NET 10 源码及锁定构建说明；可发布运行包；测试驱动/固定场景；独立 JSON/日志轨迹格式（合同版本、Session/Sequence 或握手代号、OperationId、目标、状态转移、时间、实际执行次数、最终物理状态）。 | Linux 和 GitHub Windows 均能从干净环境构建、启动、退出；同一场景重复执行结果一致；测试入口仅负责设置设备事实，业务仍经中台 Modbus 客户端。 |
| D2b 独立 3D 设备接口模拟；先于 S01 跨进程验收 | 按中台 `IPlacementLocator` 语义提供独立进程与版本化调用协议；接收 TrayRunId、RequestId、坐标批次和来源，确定性返回双槽/空槽、重复/无效槽、超时/断连、错 ID/旧盘等结果。设备侧自己记录请求、结果和实际触发次数，不复用中台状态机，也不宣称真实点云精度。接口传输方式和字段编码须先与中台共同定版；中台实现调用客户端。 | 源码/运行包、协议与固定样例、启动命令、可设置故障的控制方式、设备侧独立轨迹和用例结果。 | 中台经正式调用端口连接另一个进程；N01 与 S01–S04b 的设备轨迹可独立对账；缺此交付时中台单元/Host 端口替身测试不能标为双进程 PASS。 |
| D3 协议适配与双进程联调；先于 S01 主 PR 申请评审 | 在 `backend/src/Inspection.Infrastructure/` 实现版本化 V1.3 Modbus 编解码及设备适配，并与中台同学用实际客户端对接 D2；中台负责 Application/Host 规则和装配。不用模拟器 HTTP 控制接口冒充业务通信。覆盖新鲜/过期 Ready、未夹紧/互锁、受理后迟到/无完成、明确拒绝、断线重连、旧/错/重复/乱序完成、停止和未知动作对账。 | 已知原始报文字节的编码/解码合同测试；每个双进程用例的输入、预期、实际 Host 报文、独立 PLC 轨迹、动作实际次数、首次失败与修正复测；构建提交/模拟器版本和运行命令。 | 正常准备恰好执行一次 MoveTo3D；Accepted 不等于到位；旧/错完成不推进；Unknown 不自动重发。对应[故障矩阵](../../specs/005-initial-3d-station/test-matrix.md)的 P01/P02、M01–M04b 均有独立可复核证据。旧 V6 13 组另列，不算 V1.3 通过。 |
| D4 Windows 真机配合；在 D1 签认、实际适配和现场准入完成后 | 提供真实 PLC 程序/固件、设备连接与实际点位版本、公共 3D 位/到位判据、停止/复位操作和现场状态读取方法；协同现场人员按 W0–W5 递进测试。 | 设备/固件版本、配置与点表版本对应、现场操作记录、PLC 原始状态/事件轨迹、与中台/3D 日志对账、失败和恢复记录。 | 同一 main 候选包及 SHA-256，在实际 Windows 电脑上先只读连通，再验证禁止条件、批准的单次移动和真实到位；3D 触发前 PLC 已到位。未获批准的物理故障记 `NOT RUN`，不强制制造危险情形。 |

## 三、提交与联调规则

1. 从最新 main 建下位机工作分支；为保留首工位一个实现 PR，可向 `station-01-upper-implementation` 提交独立子 PR，经中台审查后纳入 [S01 实现 PR #7](https://github.com/NickYoung618/x/pull/7)。PR 写明所用合同版本、修改文件、复现命令、每项 PASS/FAIL/NOT RUN、模拟器独立轨迹与尚未实现的接口。不覆盖或删除旧协议报告。
2. 端口和场景使用隔离配置。固定种子、可配置监听地址/端口，CI 不依赖生产 PLC 或固定现场 IP；测试进程结束后仅清理自己创建的状态。生产配置不得在设备失败时自动切换为模拟成功。
3. 执行未知、通信超时或重启时先读真实状态并对账；无法确认则保持 `RecoveryRequired`，不自动补发动作。PLC/电气互锁仍在下位机负责，上位机的软件判断不能取代实体安全保护。
4. D4 使用[现场规程](../../specs/005-initial-3d-station/windows-real-device.md)；当前仓库没有自有 Windows runner，先由现场人员在可交互 Windows 电脑手动运行同包测试。D1、D2、D3 未通过或 3D SDK/标定未就绪时，不发真实运动命令。

## 四、派任完成的回执

请在交付时附一页回执：合同版本及签认人、代码提交/PR、模拟器构建与发布命令、测试环境/版本、各用例结果、独立动作轨迹目录、已知限制、现场待确认项及下一步责任人。D1–D3 完成可申请**S01 下位机软件联调通过**；D4 须另有现场记录才能申请**真实 PLC 联调通过**。二者均不代表完整整盘验收。
